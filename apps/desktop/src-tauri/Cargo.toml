[package]
name = "covaga-desktop"
version = "0.2.0"
description = "Tauri desktop shell wrapping the Covaga ECAD web viewer"
license = "MIT"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
